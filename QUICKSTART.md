# 🚀 Быстрый старт

## Запуск приложения

### Способ 1: Python (рекомендуется)
```bash
cd ai-chat-platform
python3 -m http.server 8888
```

Откройте: http://localhost:8888

### Способ 2: npm serve
```bash
npm install -g serve
serve -p 8888
```

### Способ 3: VS Code Live Server
Установите расширение "Live Server" и нажмите "Go Live"

---

## 📁 Структура файлов

```
ai-chat-platform/
├── index.html           # Главная страница приложения
├── css/
│   └── styles.css       # Все стили (темная тема, адаптив)
├── js/
│   └── app.js           # Вся логика приложения
├── assets/              # Изображения (пока пусто)
├── README.md            # Полная документация
├── QUICKSTART.md        # Этот файл
├── CHANGELOG.md         # История изменений
├── package.json         # Метаданные проекта
└── .gitignore          # Игнорируемые файлы
```

---

## 🎨 Основные компоненты

### HTML (index.html)
- **Header**: меню, выбор модели, профиль, баланс
- **Sidebar**: новый чат, поиск, список чатов
- **Main Area**: приветственный экран или активный чат
- **Input Area**: поле ввода, кнопки вложений, отправка
- **Modals**: выбор модели, профиль пользователя

### CSS (styles.css)
- **CSS Variables**: легкая кастомизация цветов
- **Responsive Design**: адаптивность для всех экранов
- **Animations**: плавные переходы и эффекты
- **Components**: модульные стили для всех элементов

### JavaScript (app.js)
- **AppState**: глобальное состояние приложения
- **Event Listeners**: обработчики всех действий
- **Chat Management**: создание, отображение чатов
- **Message Rendering**: Markdown, подсветка кода
- **API Simulation**: mock ответов AI (для демо)
- **Profile System**: управление профилем пользователя

---

## 🔧 Кастомизация

### Изменить цветовую схему
Отредактируйте `css/styles.css`:
```css
:root {
    --accent-primary: #10a37f;  /* Основной цвет */
    --bg-primary: #0f0f0f;      /* Фон */
    --text-primary: #ececec;    /* Текст */
}
```

### Добавить новую модель
Отредактируйте `js/app.js` → `AppState.models`:
```javascript
{
    id: 'my-model',
    name: 'My Model',
    icon: '🤖',
    description: 'Описание модели',
    price: '~1₽/1K токенов',
    context: '32K',
    capabilities: ['text', 'vision']
}
```

### Изменить начальный профиль
Отредактируйте `js/app.js` → `AppState.user.profile`

---

## 🔌 Интеграция с Backend

### Шаг 1: Замените mock API
В `app.js` найдите функцию `simulateAIResponse()` и замените на:

```javascript
async function sendToRealAPI(message, model, profile) {
    const response = await fetch('https://your-backend.com/api/chat', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${YOUR_API_KEY}`
        },
        body: JSON.stringify({
            message: message,
            model: model,
            profile: profile,
            chat_id: AppState.currentChatId
        })
    });
    
    return await response.json();
}
```

### Шаг 2: Добавьте streaming (опционально)
```javascript
async function streamResponse(message, model) {
    const response = await fetch('https://your-backend.com/api/stream', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({ message, model })
    });
    
    const reader = response.body.getReader();
    const decoder = new TextDecoder();
    
    while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        
        const chunk = decoder.decode(value);
        // Обновите UI с новыми токенами
        appendToLastMessage(chunk);
    }
}
```

### Шаг 3: Настройте аутентификацию
Добавьте функции логина/регистрации и сохраняйте JWT токен

---

## 🧪 Тестирование

### Открыть DevTools
- Chrome/Edge: `F12` или `Cmd+Option+I` (Mac)
- Firefox: `F12` или `Cmd+Option+K` (Mac)

### Проверить консоль
Все логи и ошибки появятся в консоли браузера

### Тестовые сценарии
1. Создайте новый чат
2. Отправьте несколько сообщений
3. Переключите модель
4. Отредактируйте профиль
5. Загрузите изображение
6. Проверьте адаптивность (уменьшите окно)

---

## 📱 Адаптивность

### Точки останова (breakpoints)
- Desktop: 1920px+
- Laptop: 1366px - 1920px
- Tablet: 768px - 1366px
- Mobile: 320px - 768px

### Тестирование
В DevTools нажмите иконку устройства или `Cmd+Shift+M`

---

## 🐛 Распространенные проблемы

### Не загружаются стили
Убедитесь, что пути к файлам правильные:
```html
<link rel="stylesheet" href="css/styles.css">
```

### Не работает JavaScript
Проверьте консоль на ошибки. Убедитесь, что библиотеки загружены:
- marked.js
- highlight.js

### Не работает на мобильных
Проверьте viewport meta tag в `<head>`:
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0">
```

---

## 📚 Полезные ресурсы

- [Marked.js](https://marked.js.org/) - Markdown парсер
- [Highlight.js](https://highlightjs.org/) - Подсветка кода
- [MDN Web Docs](https://developer.mozilla.org/) - Документация по веб-технологиям

---

## 💡 Советы по разработке

1. **Используйте DevTools** - для отладки и инспекции
2. **Проверяйте на разных браузерах** - Chrome, Firefox, Safari
3. **Тестируйте на мобильных** - реальных устройствах
4. **Оптимизируйте изображения** - используйте WebP
5. **Минифицируйте для продакшена** - CSS и JS

---

## 🚀 Деплой

### GitHub Pages
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin your-repo-url
git push -u origin main
```

Включите GitHub Pages в настройках репозитория

### Netlify
Просто перетащите папку на netlify.com

### Vercel
```bash
npm install -g vercel
vercel
```

---

**Готово! Начинайте разработку! 🎉**
