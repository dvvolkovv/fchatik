# 🔗 Настройка подключения к Backend

## 🚀 Быстрая настройка (2 минуты)

### Шаг 1: Получите URL вашего Backend

После деплоя backend на Railway:
1. Railway → ваш проект → Backend сервис
2. Settings → Domains
3. Скопируйте URL (например: `https://chatik-production.up.railway.app`)

### Шаг 2: Обновите API_CONFIG в Frontend

Откройте `js/app.js` и найдите в начале файла (строка ~5):

```javascript
// API Configuration
const API_CONFIG = {
    // Замените на ваш Railway URL после деплоя backend
    baseURL: 'http://localhost:8000/api/v1',
    // Production: 'https://your-backend.railway.app/api/v1'
};
```

**Замените на:**
```javascript
const API_CONFIG = {
    baseURL: 'https://your-backend.railway.app/api/v1',
};
```

### Шаг 3: Обновите CORS на Backend

В Railway Variables добавьте URL вашего frontend:

```env
CORS_ORIGINS=https://your-frontend.vercel.app,http://localhost:8888
```

### Шаг 4: Готово! ✅

Откройте frontend и:
1. Нажмите кнопку "Войти" (🔑 в правом верхнем углу)
2. Зарегистрируйтесь или войдите
3. Создайте чат
4. Отправьте сообщение
5. Получите ответ от LLM!

---

## 🧪 Тестирование локально

### Backend локально:
```bash
cd ai-chat-backend
cp .env.local .env
source venv/bin/activate
uvicorn app.main:app --reload
```

Backend работает на: `http://localhost:8000`

### Frontend локально:
```bash
cd ai-chat-platform
python3 -m http.server 8888
```

Frontend работает на: `http://localhost:8888`

### API_CONFIG для локальной разработки:
```javascript
const API_CONFIG = {
    baseURL: 'http://localhost:8000/api/v1',
};
```

---

## 🔧 Автоматическое определение окружения

Для удобства можно использовать автоопределение:

```javascript
const API_CONFIG = {
    baseURL: window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'
        ? 'http://localhost:8000/api/v1'
        : 'https://your-backend.railway.app/api/v1'
};
```

Или через переменные окружения (для Vite/React):
```javascript
const API_CONFIG = {
    baseURL: import.meta.env.VITE_API_URL || 'http://localhost:8000/api/v1'
};
```

---

## ✅ Что работает после интеграции:

### Аутентификация:
- ✅ Регистрация новых пользователей
- ✅ Вход в систему
- ✅ Сохранение JWT токена
- ✅ Автоматическое обновление UI
- ✅ Выход из системы

### Чаты:
- ✅ Создание чата через API
- ✅ Загрузка списка чатов
- ✅ Загрузка истории сообщений
- ✅ Автосохранение в БД

### Сообщения:
- ✅ Отправка сообщения к LLM
- ✅ Получение ответа от GPT-4/Claude/Gemini
- ✅ Подсчёт токенов и стоимости
- ✅ Обновление баланса

### Модели:
- ✅ Загрузка доступных моделей с backend
- ✅ OpenRouter: GPT-4, Claude, Gemini, Llama
- ✅ Переключение моделей в UI

---

## 🎯 Чек-лист интеграции:

### Frontend:
- [✅] API_CONFIG настроен
- [✅] Auth функции добавлены
- [✅] API вызовы интегрированы
- [✅] Модальное окно логина создано
- [✅] Обработка ошибок добавлена
- [✅] Обновление UI после auth

### Backend:
- [✅] Задеплоен на Railway
- [✅] PostgreSQL подключен
- [✅] Redis подключен
- [✅] OpenRouter настроен
- [✅] CORS настроен
- [✅] Миграции применены

### Проверка:
- [ ] Откройте frontend
- [ ] Нажмите "Войти"
- [ ] Зарегистрируйтесь
- [ ] Создайте чат
- [ ] Отправьте сообщение
- [ ] Получите ответ от LLM
- [ ] Проверьте обновление баланса

---

## 🆘 Troubleshooting

### Ошибка CORS
```
Access to fetch at 'https://backend...' has been blocked by CORS policy
```

**Решение:**
- Добавьте URL frontend в `CORS_ORIGINS` на Railway
- Перезапустите backend сервис
- Очистите кэш браузера (Cmd+Shift+R)

### 401 Unauthorized
```
Could not validate credentials
```

**Решение:**
- Войдите заново
- Проверьте токен: `localStorage.getItem('authToken')`
- Проверьте срок действия токена (30 минут по умолчанию)

### Сообщения не отправляются
**Проверьте:**
- DevTools → Console → есть ли ошибки?
- DevTools → Network → запросы к API
- Backend доступен: `curl https://your-backend.railway.app/health`
- OpenRouter ключ настроен на Railway

### Модели не загружаются
**Решение:**
- Проверьте `OPENROUTER_API_KEY` в Railway Variables
- Перезапустите backend
- Проверьте логи: Railway → Deployments → View Logs

---

## 💡 Полезные команды для отладки

### В DevTools Console:

```javascript
// Проверка API подключения
fetch('https://your-backend.railway.app/health')
    .then(r => r.json())
    .then(console.log);

// Проверка токена
console.log(localStorage.getItem('authToken'));

// Проверка пользователя
console.log(JSON.parse(localStorage.getItem('currentUser')));

// Проверка моделей
fetch('https://your-backend.railway.app/api/v1/llm/models')
    .then(r => r.json())
    .then(console.log);
```

---

**Frontend готов к работе с Backend! 🚀**
