# AI Chat Platform - Frontend

Персонализированная платформа для общения с различными AI моделями.

## 🚀 Возможности

- **Множество моделей**: GPT-4, Claude, Gemini, YandexGPT и другие
- **Персонализация**: Система профиля на основе ценностей, убеждений, интересов, навыков
- **Мультимодальность**: Поддержка текста, изображений, документов
- **Голосовой ввод**: 🎤 Распознавание речи с поддержкой русского и других языков
- **Темы оформления**: 🎨 Темная и светлая тема с плавным переключением
- **Современный UI**: Адаптивный дизайн, анимации
- **Markdown**: Полная поддержка форматирования и подсветки кода

## 📁 Структура проекта

```
ai-chat-platform/
├── index.html          # Главная страница
├── css/
│   └── styles.css      # Стили приложения
├── js/
│   └── app.js          # Основная логика
├── assets/             # Изображения и ресурсы
└── README.md           # Документация
```

## 🛠 Технологии

- **HTML5** - структура
- **CSS3** - стилизация (CSS Variables, Flexbox, Grid)
- **Vanilla JavaScript** - логика приложения
- **Marked.js** - рендеринг Markdown
- **Highlight.js** - подсветка синтаксиса кода

## 🎨 Особенности дизайна

- Темная тема (можно легко добавить светлую)
- Адаптивный дизайн для всех устройств
- Плавные анимации и переходы
- Интуитивный интерфейс по аналогии с ChatGPT

## 📦 Установка и запуск

### Вариант 1: Простой запуск

Просто откройте `index.html` в браузере.

### Вариант 2: Локальный сервер

```bash
# Используя Python
python3 -m http.server 8000

# Или Node.js
npx serve

# Затем откройте http://localhost:8000
```

## 🔧 Конфигурация

### Добавление новых моделей

Отредактируйте массив `AppState.models` в `js/app.js`:

```javascript
{
    id: 'model-id',
    name: 'Название модели',
    icon: '🤖',
    description: 'Описание',
    price: '~цена/1K токенов',
    context: 'размер контекста',
    capabilities: ['text', 'vision', 'code']
}
```

### Изменение темы

Отредактируйте CSS переменные в `css/styles.css`:

```css
:root {
    --bg-primary: #0f0f0f;
    --accent-primary: #10a37f;
    /* и т.д. */
}
```

## 🔌 Интеграция с Backend

Для подключения к реальному API (n8n или другому):

1. Замените функцию `simulateAIResponse()` в `app.js`
2. Добавьте реальные HTTP запросы к вашему backend:

```javascript
async function sendMessageToAPI(message, model) {
    const response = await fetch('https://your-api.com/chat', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': 'Bearer YOUR_TOKEN'
        },
        body: JSON.stringify({
            message: message,
            model: model,
            profile: AppState.user.profile
        })
    });
    
    return await response.json();
}
```

3. Для streaming используйте Server-Sent Events (SSE) или WebSocket:

```javascript
const eventSource = new EventSource('https://your-api.com/stream');
eventSource.onmessage = (event) => {
    const data = JSON.parse(event.data);
    // Обновляйте UI по мере поступления токенов
};
```

## 📱 Адаптивность

Приложение полностью адаптивно:
- Desktop (1920x1080+)
- Laptop (1366x768+)
- Tablet (768px+)
- Mobile (320px+)

## 🎯 TODO

- [ ] Интеграция с реальным API
- [ ] Светлая тема
- [ ] Экспорт чатов (PDF, Markdown, JSON)
- [ ] Папки для организации чатов
- [ ] Поделиться чатом (ссылка)
- [ ] Голосовой ввод
- [ ] Поддержка PWA
- [ ] Офлайн режим

## 📝 Лицензия

MIT License

## 🤝 Вклад

Приветствуются pull requests и issues!

## 📧 Контакты

Для вопросов и предложений: support@example.com

---

**Сделано с ❤️ для персонализированного AI опыта**
