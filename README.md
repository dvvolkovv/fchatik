# 🤖 AI Chat Platform - Frontend

Современный веб-интерфейс для общения с различными LLM моделями.

## ✨ Возможности

- 🎯 **20+ LLM моделей** (GPT-5, Claude, Gemini, DeepSeek и др.)
- 💬 **Streaming ответов** в реальном времени
- 👤 **Автоматический сбор профиля** пользователя
- 🎨 **Темная/светлая** тема
- 🎤 **Голосовой ввод**
- 📱 **Responsive дизайн**
- 💾 **История чатов**

## 🚀 Деплой на Railway

### 1. Создайте сервис

1. Откройте https://railway.app/
2. **+ New** → **GitHub Repo** → `dvvolkovv/fchatik`

### 2. Добавьте переменную окружения

В **Variables** добавьте:

```env
API_BASE_URL=https://your-backend.railway.app
```

где `your-backend.railway.app` - ваш Railway backend URL.

### 3. Получите домен

**Settings** → **Domains** → **Generate Domain**

Готово! 🎉

## 🛠️ Локальная разработка

```bash
# Создайте config.js для локальной разработки
cat > config.js << 'EOF'
const CONFIG = {
    API_BASE_URL: 'http://localhost:8000',
    API_PREFIX: '/api/v1'
};
window.CONFIG = CONFIG;
EOF

# Запустите локальный сервер
python3 -m http.server 8889
```

Откройте http://localhost:8889

## 📋 Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `API_BASE_URL` | URL backend API | `https://backend.railway.app` |

## 🐳 Docker

```bash
# Build
docker build -t ai-chat-frontend .

# Run с переменной окружения
docker run -p 80:80 -e API_BASE_URL=https://your-backend.railway.app ai-chat-frontend
```

## 📁 Структура

```
ai-chat-platform/
├── index.html              # Главная страница
├── config.template.js      # Шаблон конфига (для Docker)
├── css/
│   └── styles.css         # Стили приложения
├── js/
│   └── app.js            # Основная логика
├── assets/               # Изображения, иконки
├── Dockerfile           # Docker контейнер
├── nginx.conf          # Nginx конфигурация
├── docker-entrypoint.sh # Entrypoint для подстановки env
└── railway.toml        # Railway конфигурация
```

## 🔧 Технологии

- **HTML5** + **CSS3** + **Vanilla JavaScript**
- **Marked.js** - рендеринг Markdown
- **Highlight.js** - подсветка кода
- **Web Speech API** - голосовой ввод
- **Nginx** - веб-сервер для продакшена

## 📝 Лицензия

MIT

## 🆘 Поддержка

Создайте issue в репозитории: https://github.com/dvvolkovv/fchatik/issues
